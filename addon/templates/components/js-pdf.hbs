{{yield (hash
  filename=filename
  content=content
  save=(action "onSave")
  createPdfInstance=(action 'createPdfInstance')
)}}

{{#if showPdf}}
  <iframe class="ember-js-pdf__frame" width={{width}} height={{height}} src={{src}} />
{{/if}}
